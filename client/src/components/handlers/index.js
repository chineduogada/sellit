export const handleLoginClick = (history) => {
	history.push("/login");
};
export const handleSignupClick = (history) => {
	history.push("/signup");
};
export const handleProfileClick = (history) => {
	history.push("/profile");
};
export const handleProductsClick = (history) => {
	history.push("/products");
};
export const handleSellItClick = (history) => {
	history.push("/products/sell");
};

